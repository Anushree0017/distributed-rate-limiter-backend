"""Test-only helpers."""


class FakeClock:
    """Injectable clock so algorithm tests control time deterministically."""

    def __init__(self, start: float = 0.0) -> None:
        self._now = start

    def __call__(self) -> float:
        return self._now

    def advance(self, seconds: float) -> None:
        self._now += seconds
